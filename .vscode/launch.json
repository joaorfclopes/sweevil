{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Start",
      "type": "node-terminal",
      "request": "launch",
      "command": "lsof -ti:3000,4040,5000,8080 2>/dev/null | xargs kill -9 2>/dev/null; npm start",
      "cwd": "${workspaceFolder}"
    }
  ]
}
