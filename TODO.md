# TODO

## Pre-deploy smoke test suite (CI gate before Heroku push)
The deploy workflow (`.github/workflows/deploy.yml`) currently pushes to Heroku immediately after a successful build with no runtime verification. Add a test job that runs before the deploy step and blocks it on failure. Coverage should include: (1) **auth** — login with valid credentials returns a JWT, invalid credentials return 401; (2) **products** — create a product (POST `/api/products`), verify it appears in the list, delete it; (3) **bookings** — create a booking (POST `/api/bookings`), verify it persists, cancel it; (4) **image upload** — POST a small JPEG to `/api/uploads/s3`, verify an S3 URL is returned; (5) **payment intent creation** — POST to the order/booking payment-intent route, verify a `clientSecret` is returned (Stripe test-mode key required in CI secrets); (6) **webhook signature** — send a synthetic Stripe webhook event with a valid `stripe-signature` header, verify 200 and that the handler fires correctly. Use Jest + Supertest against a real test MongoDB (MongoDB Memory Server) and real Stripe test-mode keys stored as GitHub Actions secrets (`STRIPE_TEST_SECRET_KEY`, `MONGO_TEST_URI`, `JWT_SECRET`, `AWS_*`). Wire the new `test` job into `deploy.yml` as a `needs: test` dependency on the existing deploy job so a failed test blocks the push.

## Refund management (admin-controlled)
Refunds are always admin-initiated — no automatic refund when a client cancels. Rules: (1) **Admin cancels an order** → automatically call `stripe.refunds.create({ payment_intent: order.paymentResult.id })` inside `PUT /api/orders/:id/cancel`, guarded by `isPaid === true`; send cancellation + refund confirmation emails to client and admin using the existing cancel email templates (update copy to say "refund initiated"); (2) **Client cancels an order** → no automatic refund; instead, surface a **Refund** button next to that order row in the admin orders table — clicking it calls a new `POST /api/orders/:id/refund` route (admin-only) that triggers `stripe.refunds.create()` and marks `order.isRefunded = true`; (3) **Booking cancellations** → same pattern: admin cancel triggers automatic refund, no client-facing cancel path exists (by design). Building blocks: `paymentResult.id` holds the `paymentIntentId` on both models, cancel routes exist, nodemailer + cancel templates exist. New field needed: `isRefunded: Boolean` on both models to prevent double-refunds.

## Booking confirmation page + client cancel flow
After a client completes a booking payment, redirect to a `/booking/:id/confirm?token=...` page (mirroring `OrderScreen.jsx`) that shows booking details: date, slot, guest info, price, status. The page is public but token-gated (same `confirmToken` pattern as orders). Add a **Cancel booking** button that: (1) shows a modal warning the client that cancellations are not refunded — admin handles refunds separately if applicable; (2) on confirm, calls `PUT /api/bookings/:id/cancel` with the token; (3) updates the UI to show cancelled status. Backend change needed: the current booking cancel route (`bookingRoute.js:291`) is `isAuth + isAdmin` — add a parallel token-based path (or extend the existing route) that accepts `confirmToken` for client self-cancellation, matching the order cancel pattern at `orderRoute.js:408`.

## Migrate from Heroku to Vercel
Backend is Express + Mongoose running as a persistent server process — incompatible with Vercel's serverless model out of the box. Migration steps: (1) wrap `server.js` as a Vercel serverless function in `api/index.js`; (2) add `vercel.json` with rewrite rules routing `/api/*` to the function and `/*` to the built frontend; (3) handle Mongoose cold-start reconnection (can't rely on a persistent connection); (4) remove the Heroku-specific `app.set('trust proxy', 1)` or replace with Vercel's equivalent; (5) remove `Procfile` and `app.json`. Frontend static files go to Vercel CDN automatically via the build output. Stripe webhooks raw body handling is already in place. Do the async image processing Lambda (see below) before this, since sharp processing will hit Vercel's 10s serverless timeout otherwise.

## Async image processing via S3 Lambda
Currently `uploadRoute.js` POST `/s3` runs sharp synchronously inside the request (resize + AVIF encode) before returning the S3 URL. With `effort: 2` this stays under Heroku's 30s timeout, but quality is capped. Migration: (1) receive file, upload raw buffer to S3 as `pending/{folder}/{uuid}.orig` with folder encoded in object metadata, return a `{ jobId }` immediately; (2) S3 event notification triggers a Lambda on the `pending/` prefix — Lambda runs sharp at `effort: 6, quality: 65`, writes final AVIF to `{folder}/{uuid}.avif`, deletes the pending file; (3) add `GET /api/uploads/status/:jobId` backend endpoint that checks whether `{folder}/{uuid}.avif` exists in S3; (4) frontend polls that endpoint until URL is ready, then proceeds to save the gallery/product record. Affects `GalleryAdminTab.jsx` upload flow and `ProductEditScreen.jsx`. Required before Vercel migration — Vercel's 10s function timeout makes synchronous sharp encoding impossible for large images.
